export const name = 'Item Cards';

export const importLabel = `Import ${name} JSON`;
export const exportLabel = (suffix: string) => `Export ${name} ${suffix}`;

export default { name, importLabel, exportLabel };
